# Core

Estructura futura para modelos puros, estados compartidos y reglas de negocio
que deban ser usadas por el worker, el admin API y el dashboard.

Por ahora no contiene logica funcional y no reemplaza a `services/`, `flows/`,
`browser/` ni `utils/`. La migracion real se hara por fases documentadas antes
de mover codigo.
